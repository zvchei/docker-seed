services:
    git:
        hostname: ${PROJECT}-git
        extends:
            file: ../common/docker-compose.yaml
            service: base
        volumes:
            - root:/home/${CONTAINER_USER}/${PROJECT}
        build:
            context: .
            additional_contexts:
                localhost/base: service:base
                ssh_keys: ../../secrets/ssh
            args:
                GIT_AUTHOR_NAME: ${GIT_AUTHOR_NAME}
                GIT_AUTHOR_EMAIL: ${GIT_AUTHOR_EMAIL}

volumes:
    root:
