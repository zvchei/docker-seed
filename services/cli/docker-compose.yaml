services:
    cli:
        hostname: ${PROJECT}-cli
        extends:
            file: ../../common/docker-compose.yaml
            service: base
        volumes:
            - root:/home/${CONTAINER_USER}/${PROJECT}
        build:
            context: .
            additional_contexts:
                localhost/base: service:base

volumes:
    root:
